name: 'Kinda Contribute'
description: 'Simulates your contribution'
author: 'Th3Un1q3'
inputs:
  commits:
    description: 'How many commits to make per run'
    required: false
    default: '5'

runs:
  using: "composite"
  steps:
    - run: |
        echo ${{ toJson(github) }}
      shell: bash
      name: 'Display GitHub context'
      # TODO: define limits for dates
    - run: generate-activity.sh
      shell: bash
    - run: git status
      shell: bash
